apiVersion: v1
kind: Service
metadata:
  name: remediation-server
  namespace: k8sgptclient
spec:
  selector:
    app: remediation-server
  ports:
  - port: 9000
    targetPort: 9000
  type: ClusterIP